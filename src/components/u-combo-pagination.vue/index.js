import { UComboPagination as OComboPagination } from 'proto-ui.vusion';

export const UComboPagination = {
    name: 'u-combo-pagination',
    extends: OComboPagination,
};

export default UComboPagination;
