import { UFormTableView as OFormTableView } from 'proto-ui.vusion';

export const UFormTableView = {
    name: 'u-form-table-view',
    extends: OFormTableView,
};

export { UFormTableViewRow } from './row.vue';

export default UFormTableView;
