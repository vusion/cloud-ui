import { UStep } from '../../u-steps.vue';

export const USelectableStep = {
    name: 'u-selectable-step',
    parentName: 'u-selectable-steps',
    extends: UStep,
};

export default USelectableStep;
